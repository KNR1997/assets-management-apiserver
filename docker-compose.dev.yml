version: "3.9"

services:
  web:
    image: node:18-alpine
    container_name: assets-mgmt-web-dev
    working_dir: /app
    command: npm run dev -- --host
    volumes:
      - ./web:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
